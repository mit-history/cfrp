<!DOCTYPE html>
<html>
<head>
  <title>Com&#233;die-Fran&#231;aise Registers Project</title>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <%= yield %>
  </div>

  <div id="footer">
    <!-- Bootstrap Navbar -->
    <div class="navbar navbar-fixed-bottom">
      <div class="navbar-inner">
        <div class="container">
          <a class='brand' href="<%= url_for :root %>">Com&#233;die-Fran&#231;aise Registers Project</a>

	  <ul class='nav pull-right'>
	    <%# Can't figure out how to add this charset to file without making the *server* barf?  What am I doing wrong? %>
            <!-- li><%= link_to "Registers List", :registers %></li -->
          <% if current_user %>
	    <% if current_user.has_role? :admin %>
	    <li><%= link_to "User Administration", :user_sets %></li>
            <% end %>
	    <li class='active'><a href="#">Logged in as: <%= current_user.first_name %> <%= current_user.last_name %> (<%= current_user.email %>)</a></li>
            <li><%= link_to "Log Out", :destroy_user_session %></li>
          <% else %>
	    <li class='active'><%= link_to "Sign In", :new_user_session %></li>
          <% end %>
	  </ul>
	</div>
      </div>
    </div>
    <!-- Footer -->
  </div>
</body>
</html>
