<!DOCTYPE html>
<html>
<head>
  <title>Com&#233;die-Fran&#231;aise Registers Project</title>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="header">
  </div>

  <div id="container">
    <%= yield %>
  </div>

  <div id="footer">
    <div class="navbar navbar-fixed-bottom">
      <div class="navbar-inner">
	<div class="container">
          <a class='brand' href="<%= url_for :root %>">Com&#233;die-Fran&#231;aise Registers Project</a>

	  <ul class='nav pull-right'>
	    <%# Can't figure out how to add this charset to file without making the *server* barf?  What am I doing wrong? %>
            <li><%= link_to "Registers List", :registers %></li>
	    <% if current_user %>
	    <!-- li><%#= link_to "Manage Users", :users %></li -->
	    <li class="logout">Logged in as:
              <span class='user_info'><%= current_user.first_name %> <%= current_user.last_name %></span>
	      (<span class='user_info'><%= current_user.email %></span>)
	      <%= link_to "Log Out", :destroy_user_session %></li>
	    <% end %>
	  </ul>
	</div>
      </div>
    </div>
  </div>
</body>
</html>
